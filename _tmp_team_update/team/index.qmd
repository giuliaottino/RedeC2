---
title: "Team - Rede C2"
subtitle: "People and institutional links across Rede C2"
format:
  html:
    toc: false
    page-layout: full
execute:
  echo: false
  eval: true
  freeze: false
  cache: false
  warning: false
  message: false
resources:
  - ../figures/
  - ../_input_data/C2_team_images/
  - ../_input_data/icons/
---

```{r setup, include=FALSE}
library(dplyr)
library(stringr)
library(htmltools)
library(openxlsx)

team_data <- openxlsx::read.xlsx("../_input_data/C2_team.xlsx")
names(team_data) <- make.names(names(team_data))

clean_file_name <- function(x) {
  x <- tolower(iconv(x, from = "UTF-8", to = "ASCII//TRANSLIT"))
  x <- str_replace_all(x, "[^a-z0-9]+", "_")
  x <- str_replace_all(x, "_+", "_")
  x <- str_replace_all(x, "^_|_$", "")
  paste0(x, ".jpg")
}

default_team_photo <- "../figures/redec2_hex_sticker_.png"

team_data <- team_data %>%
  mutate(
    first_letter = toupper(str_sub(Member, 1, 1)),
    photo_file = clean_file_name(Member),
    photo_exists = file.exists(file.path("../_input_data/C2_team_images", photo_file)),
    photo_path = ifelse(
      photo_exists,
      paste0("../_input_data/C2_team_images/", photo_file),
      default_team_photo
    ),
    photo_class = ifelse(photo_exists, "team-photo", "team-photo team-photo-default"),
    photo_wrap_class = ifelse(photo_exists, "team-photo-wrap", "team-photo-wrap team-photo-wrap-default")
  ) %>%
  arrange(Member)
```

```{=html}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800;900&family=Source+Serif+4:opsz,wght@8..60,400;8..60,600;8..60,700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
```

```{=html}
<style>
:root {
  --rc2-red: #A93632;
  --rc2-red-dark: #6E1F1D;
  --rc2-green: #626A38;
  --rc2-green-dark: #2E3B24;
  --rc2-green-soft: #D7DDC5;
  --rc2-ochre: #D99A27;
  --rc2-cream: #F6F1E8;
  --rc2-paper: #FFFDF7;
  --rc2-ink: #171717;
  --rc2-muted: #675F55;
  --rc2-line: rgba(98,106,56,0.22);
  --rc2-shadow: 0 16px 34px rgba(0,0,0,0.12);

  --rc2-safe-gutter: clamp(0.75rem, 2.6vw, 2.25rem);
  --rc2-content-max: 1180px;
  --rc2-wide-max: 1480px;
}

*, *::before, *::after {
  box-sizing: border-box;
}

html,
body {
  width: 100% !important;
  max-width: 100% !important;
  overflow-x: clip !important;
  scroll-behavior: smooth;
}

@supports not (overflow: clip) {
  html,
  body {
    overflow-x: hidden !important;
  }
}

body {
  margin: 0 !important;
  background:
    linear-gradient(rgba(246,241,232,0.90), rgba(246,241,232,0.86)),
    url("../figures/c2_bg.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  background-attachment: fixed;
  background-position: center top;
  color: var(--rc2-ink);
  font-family: 'Montserrat', Arial, sans-serif;
}

img,
video,
iframe,
svg,
canvas {
  max-width: 100%;
}

a {
  color: var(--rc2-red);
}

#quarto-content,
#quarto-document-content,
main,
main.content,
.page-columns,
.page-columns .content {
  min-width: 0 !important;
  max-width: none !important;
  overflow-x: visible !important;
}

#quarto-content,
#quarto-document-content,
main,
main.content,
.page-columns,
.page-columns .content {
  margin-left: 0 !important;
  margin-right: 0 !important;
  padding-left: 0 !important;
  padding-right: 0 !important;
}

main.content {
  background: transparent;
  width: 100% !important;
  padding: 0 0 3.5rem 0 !important;
}

.quarto-title-block,
#title-block-header {
  display: none !important;
}

/* Navbar */
.navbar {
  box-shadow: 0 2px 18px rgba(0,0,0,0.10);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

.navbar .navbar-brand,
.navbar .nav-link {
  font-family: 'Montserrat', Arial, sans-serif;
}

.navbar .nav-link.active-section {
  color: var(--rc2-red) !important;
  font-weight: 800;
  border-bottom: 2px solid var(--rc2-red);
}

/* =========================================================
   HERO · clean, full frame
   ========================================================= */
.rc2-page-hero {
  position: relative;
  left: 50%;
  right: 50%;
  width: 100vw !important;
  max-width: 100vw !important;
  min-height: calc(100svh - 88px);
  margin-top: 0 !important;
  margin-bottom: 0 !important;
  margin-left: -50vw !important;
  margin-right: -50vw !important;
  display: grid;
  align-items: center;
  overflow: hidden;
  isolation: isolate;
  color: #fff;
  background:
    linear-gradient(108deg, rgba(23,23,23,0.58) 0%, rgba(46,59,36,0.38) 48%, rgba(110,31,29,0.25) 100%),
    url("../figures/c2_bg.jpg");
  background-size: cover;
  background-position: center center;
  background-repeat: no-repeat;
}

.page-columns .content > .rc2-page-hero,
#quarto-document-content > .rc2-page-hero,
main.content > .rc2-page-hero {
  grid-column: screen-start / screen-end !important;
}

.rc2-page-hero::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: 0;
  background:
    linear-gradient(
      180deg,
      rgba(0,0,0,0.18) 0%,
      rgba(0,0,0,0.05) 48%,
      rgba(0,0,0,0.34) 100%
    );
  pointer-events: none;
}

.rc2-page-hero-inner {
  position: relative;
  z-index: 2;
  width: min(1120px, calc(100% - var(--rc2-safe-gutter) * 2));
  margin: 0 auto;
  padding: clamp(2.4rem, 6vh, 4.5rem) 0;
}

.rc2-title-rule {
  width: min(300px, 46vw);
  height: 5px;
  margin: 0 0 clamp(1rem, 2vw, 1.35rem);
  border-radius: 999px;
  background: linear-gradient(90deg, var(--rc2-ochre), rgba(246,241,232,0.24));
  animation: fadeSlideUp 0.75s 0.08s ease both;
}

.rc2-page-hero h1 {
  margin: 0;
  max-width: 1000px;
  color: #fff;
  font-family: 'Montserrat', Arial, sans-serif;
  font-size: clamp(3.6rem, 8.4vw, 8.2rem);
  line-height: 0.88;
  font-weight: 900;
  letter-spacing: -0.085em;
  text-transform: uppercase;
  text-shadow: 0 12px 36px rgba(0,0,0,0.50);
  animation: fadeSlideUp 0.75s 0.16s ease both;
}

.rc2-page-hero h1 span {
  display: block;
  color: var(--rc2-ochre);
}

.rc2-page-hero p {
  max-width: 860px;
  margin: clamp(1rem, 2vw, 1.25rem) 0 0;
  color: rgba(255,253,247,0.97);
  font-family: 'Source Serif 4', Georgia, serif;
  font-size: clamp(1.18rem, 2.15vw, 1.9rem);
  line-height: 1.25;
  font-weight: 700;
  text-shadow: 0 4px 18px rgba(0,0,0,0.48);
  animation: fadeSlideUp 0.75s 0.24s ease both;
}

/* =========================================================
   SECTIONS
   ========================================================= */
.rc2-section,
.content-card {
  width: min(var(--rc2-content-max), calc(100% - var(--rc2-safe-gutter) * 2));
  margin: 2.4rem auto 0;
  padding: clamp(1.35rem, 3vw, 2.5rem);
  background:
    radial-gradient(circle at 100% 0%, rgba(217,154,39,0.08), transparent 32%),
    linear-gradient(180deg, rgba(255,253,247,0.97), rgba(246,241,232,0.90));
  border: 1px solid rgba(98,106,56,0.18);
  border-top: 5px solid var(--rc2-green);
  box-shadow: var(--rc2-shadow);
  overflow: hidden;
}

.rc2-section-title {
  display: grid;
  grid-template-columns: 90px 1fr;
  gap: 0.8rem;
  align-items: center;
  margin-bottom: 1.35rem;
}

.rc2-section-title::before {
  content: "";
  height: 1px;
  background: var(--rc2-ink);
  opacity: 0.72;
}

.rc2-section-title h2 {
  margin: 0;
  padding-bottom: 0.45rem;
  border-bottom: 1px solid rgba(98,106,56,0.18);
  color: var(--rc2-red);
  font-family: 'Montserrat', Arial, sans-serif;
  font-size: clamp(1.25rem, 3vw, 2.1rem);
  font-weight: 900;
  line-height: 1.05;
  letter-spacing: -0.045em;
  text-transform: uppercase;
}

.rc2-text {
  max-width: 980px;
}

.rc2-lead,
.lead {
  margin: 0 0 1.15rem 0 !important;
  padding-left: 1rem;
  border-left: 5px solid var(--rc2-ochre);
  color: #191711 !important;
  font-family: 'Source Serif 4', Georgia, serif !important;
  font-size: clamp(1.08rem, 2vw, 1.3rem) !important;
  line-height: 1.65 !important;
}

.rc2-section p,
.content-card p {
  color: var(--rc2-ink);
  font-size: 0.98rem;
  line-height: 1.75;
}

/* =========================================================
   TEAM CONTROLS
   ========================================================= */
.team-controls {
  width: min(var(--rc2-content-max), calc(100% - var(--rc2-safe-gutter) * 2));
  margin: 2.4rem auto 0;
  padding: 0.95rem;
  border-radius: 22px;
  background: rgba(255,253,247,0.94);
  border: 1px solid rgba(98,106,56,0.18);
  box-shadow: 0 12px 26px rgba(0,0,0,0.10);
  display: grid;
  grid-template-columns: minmax(260px,0.58fr) minmax(0,1fr);
  gap: 1rem;
  align-items: center;
}

.team-search {
  width: 100%;
  border-radius: 999px;
  border: 1px solid rgba(98,106,56,0.28);
  background: rgba(246,241,232,0.92);
  padding: 0.72rem 1rem;
  color: var(--rc2-ink);
  font-family: 'Montserrat', Arial, sans-serif;
}

.team-search:focus {
  outline: 2px solid rgba(169,54,50,0.22);
  outline-offset: 2px;
}

.team-index {
  display: flex;
  flex-wrap: wrap;
  gap: 0.3rem;
  justify-content: flex-end;
}

.team-index a,
.team-index span {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 1.85rem;
  height: 1.85rem;
  border-radius: 999px;
  font-family: 'Space Mono', monospace;
  font-size: 0.66rem;
  font-weight: 800;
  text-decoration: none;
}

.team-index a {
  background: rgba(169,54,50,0.08);
  border: 1px solid rgba(169,54,50,0.24);
  color: var(--rc2-red);
}

.team-index span {
  background: rgba(98,106,56,0.08);
  border: 1px solid rgba(98,106,56,0.12);
  color: rgba(103,95,85,0.45);
}

/* =========================================================
   TEAM GRID
   ========================================================= */
.team-group {
  width: min(var(--rc2-wide-max), calc(100% - var(--rc2-safe-gutter) * 2));
  margin: 1.25rem auto 0;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(min(100%, 260px), 1fr));
  gap: 1rem;
}

.team-card {
  position: relative;
  min-width: 0;
  overflow: hidden;
  border-radius: 24px;
  background: linear-gradient(180deg, rgba(255,253,247,0.97), rgba(246,241,232,0.90));
  border: 1px solid rgba(98,106,56,0.18);
  box-shadow: 0 12px 26px rgba(0,0,0,0.10);
  transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
}

.team-card:hover {
  transform: translateY(-3px);
  box-shadow: 0 18px 34px rgba(0,0,0,0.14);
  border-color: rgba(169,54,50,0.28);
}

.team-photo-wrap {
  position: relative;
  height: clamp(230px, 23vw, 340px);
  background:
    radial-gradient(circle at 50% 20%, rgba(217,154,39,0.18), transparent 36%),
    linear-gradient(180deg, rgba(98,106,56,0.18), rgba(246,241,232,0.92));
  overflow: hidden;
}

.team-photo {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  filter: saturate(0.96) contrast(1.03);
  transition: transform 0.8s ease;
}

.team-photo-default {
  object-fit: contain;
  padding: clamp(2rem, 4vw, 3rem);
  filter: drop-shadow(0 12px 24px rgba(0,0,0,0.12));
}

.team-card:hover .team-photo {
  transform: scale(1.035);
}

.team-photo-wrap::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(23,23,23,0) 50%, rgba(23,23,23,0.28) 100%);
  pointer-events: none;
}

.team-photo-wrap-default::after {
  display: none;
}

.team-letter {
  position: absolute;
  top: 0.8rem;
  left: 0.8rem;
  z-index: 2;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 2.15rem;
  height: 2.15rem;
  border-radius: 999px;
  background: rgba(46,59,36,0.84);
  color: var(--rc2-ochre);
  font-family: 'Space Mono', monospace;
  font-weight: 900;
  border: 1px solid rgba(246,241,232,0.24);
  backdrop-filter: blur(6px);
}

.team-info {
  padding: 1rem;
}

.team-name {
  margin: 0;
  color: var(--rc2-green-dark);
  font-size: clamp(1.05rem,1.4vw,1.28rem);
  line-height: 1.12;
  font-weight: 900;
  letter-spacing: -0.04em;
}

.team-affiliation {
  margin: 0.55rem 0 0 0 !important;
  color: var(--rc2-muted) !important;
  font-size: 0.86rem !important;
  line-height: 1.45 !important;
}

.team-icons {
  display: flex;
  flex-wrap: wrap;
  gap: 0.45rem;
  margin: 0.75rem 0 0 0;
}

.icon-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 2rem;
  height: 2rem;
  border-radius: 999px;
  background: rgba(246,241,232,0.92);
  border: 1px solid rgba(98,106,56,0.20);
  transition: transform 0.16s ease, background 0.16s ease;
}

.icon-link:hover {
  transform: translateY(-1px);
  background: #fff;
}

.icon-link img {
  width: 1rem;
  height: 1rem;
  object-fit: contain;
}

.team-empty {
  display: none;
  width: min(900px, calc(100% - var(--rc2-safe-gutter) * 2));
  margin: 1rem auto;
  padding: 1rem;
  border-radius: 18px;
  background: rgba(169,54,50,0.08);
  border: 1px dashed rgba(169,54,50,0.30);
  color: var(--rc2-red-dark);
}

/* =========================================================
   ANIMATION
   ========================================================= */
@keyframes fadeSlideUp {
  from { opacity: 0; transform: translateY(24px); }
  to   { opacity: 1; transform: translateY(0); }
}

.reveal-ready {
  opacity: 0;
  transform: translateY(38px);
  transition: opacity 0.72s ease, transform 0.72s cubic-bezier(.2,.7,.2,1);
  transition-delay: var(--reveal-delay, 0ms);
  will-change: opacity, transform;
}

.reveal-ready.is-visible {
  opacity: 1;
  transform: translateY(0);
}

@media (prefers-reduced-motion: reduce) {
  .reveal-ready {
    opacity: 1 !important;
    transform: none !important;
    transition: none !important;
  }
}

/* =========================================================
   RESPONSIVE
   ========================================================= */
@media (max-height: 760px) and (min-width: 900px) {
  .rc2-page-hero h1 {
    font-size: clamp(3.2rem, 7.2vw, 6.8rem);
  }

  .rc2-page-hero p {
    font-size: clamp(1.05rem, 1.85vw, 1.6rem);
  }
}

@media (max-width: 980px) {
  .team-controls {
    grid-template-columns: 1fr;
  }

  .team-index {
    justify-content: flex-start;
  }

  .rc2-section-title {
    grid-template-columns: 56px 1fr;
  }
}

@media (max-width: 760px) {
  .rc2-page-hero {
    min-height: calc(100svh - 76px);
  }

  .rc2-page-hero h1 {
    font-size: clamp(3rem, 15vw, 5.4rem);
  }

  .team-group {
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 230px), 1fr));
  }

  .rc2-section-title {
    grid-template-columns: 34px 1fr;
  }
}

@media (max-width: 420px) {
  :root { --rc2-safe-gutter: 0.65rem; }

  .rc2-section,
  .content-card {
    padding: clamp(1rem, 4vw, 1.35rem);
    border-radius: 22px;
  }
}
</style>
```

```{=html}
<section class="rc2-page-hero" id="team-hero" aria-label="RedeC2 team">
  <div class="rc2-page-hero-inner">
    <div class="rc2-title-rule" aria-hidden="true"></div>

    <h1>
      RedeC2
      <span>team</span>
    </h1>

    <p>
      People and institutional links supporting Caatinga and Cerrado monitoring
      across Rede C2.
    </p>
  </div>
</section>
```

```{=html}
<section class="rc2-section" id="team-overview">
  <div class="rc2-section-title"><h2>People and institutions</h2></div>

  <div class="rc2-text">
    <p class="rc2-lead">
      Rede C2 brings together researchers, students and institutions working on
      long-term vegetation monitoring, floristics, functional traits,
      restoration, biodiversity synthesis and conservation of seasonally dry
      ecosystems.
    </p>

    <p>
      The team list presents contact links, academic profiles and institutional
      affiliations associated with the network.
    </p>
  </div>
</section>
```

```{r generate-team-cards, echo=FALSE}
get_value <- function(member, possible_names, default = "") {
  for (name in possible_names) {
    if (name %in% names(member) && !is.na(member[[name]]) && member[[name]] != "") {
      return(as.character(member[[name]]))
    }
  }
  default
}

generate_member_card <- function(member) {
  email <- get_value(member, c("Email", "email", "E.mail", "E.mail."))
  institution <- get_value(member, c("Institution", "Instittution", "Institution.", "Affiliation"), "Affiliation not specified")
  lattes <- get_value(member, c("Lattes", "Lattes.", "lattes"))
  orcid <- get_value(member, c("ORCID", "Orcid", "orcid", "ORCID."))
  scholar <- get_value(member, c("Google.Scholar", "Google.Scholar.", "GoogleScholar", "Scholar"))
  website <- get_value(member, c("Personal.Website", "Personal.Website.", "Website", "PersonalWebsite"))

  icon_html <- ""

  if (email != "") {
    icon_html <- paste0(
      icon_html,
      '<a class="icon-link" href="mailto:', htmlEscape(email), '" aria-label="Email">',
      '<img src="../_input_data/icons/_email.png" alt="Email"></a>'
    )
  }

  if (lattes != "") {
    icon_html <- paste0(
      icon_html,
      '<a class="icon-link" href="', htmlEscape(lattes), '" target="_blank" rel="noopener" aria-label="Lattes">',
      '<img src="../_input_data/icons/_lattes.png" alt="Lattes"></a>'
    )
  }

  if (orcid != "") {
    icon_html <- paste0(
      icon_html,
      '<a class="icon-link" href="', htmlEscape(orcid), '" target="_blank" rel="noopener" aria-label="ORCID">',
      '<img src="../_input_data/icons/_orcid.png" alt="ORCID"></a>'
    )
  }

  if (scholar != "") {
    icon_html <- paste0(
      icon_html,
      '<a class="icon-link" href="', htmlEscape(scholar), '" target="_blank" rel="noopener" aria-label="Google Scholar">',
      '<img src="../_input_data/icons/_scholar.png" alt="Google Scholar"></a>'
    )
  }

  if (website != "") {
    icon_html <- paste0(
      icon_html,
      '<a class="icon-link" href="', htmlEscape(website), '" target="_blank" rel="noopener" aria-label="Website">',
      '<img src="../_input_data/icons/website.png" alt="Website"></a>'
    )
  }

  paste0(
    '<article class="team-card" data-name="', htmlEscape(tolower(member$Member)),
    '" data-institution="', htmlEscape(tolower(institution)),
    '" id="letter-', htmlEscape(member$first_letter), '">',
      '<div class="', htmlEscape(member$photo_wrap_class), '">',
        '<span class="team-letter">', htmlEscape(member$first_letter), '</span>',
        '<img src="', htmlEscape(member$photo_path), '" alt="', htmlEscape(member$Member), '" class="', htmlEscape(member$photo_class), '" loading="lazy" decoding="async">',
      '</div>',
      '<div class="team-info">',
        '<h3 class="team-name">', htmlEscape(member$Member), '</h3>',
        '<p class="team-affiliation">', htmlEscape(institution), '</p>',
        '<div class="team-icons">', icon_html, '</div>',
      '</div>',
    '</article>'
  )
}

letters_present <- unique(toupper(team_data$first_letter))

letter_links <- sapply(
  LETTERS,
  function(letter) {
    if (letter %in% letters_present) {
      paste0('<a href="#letter-', letter, '">', letter, '</a>')
    } else {
      paste0('<span>', letter, '</span>')
    }
  }
)

all_cards <- paste(
  vapply(seq_len(nrow(team_data)), function(i) generate_member_card(team_data[i, ]), character(1)),
  collapse = "\n"
)

HTML(
  paste0(
    '<div class="team-controls">',
      '<input type="text" id="team-search" class="team-search" placeholder="Search by name or institution..." aria-label="Search team by name or institution">',
      '<div class="team-index" aria-label="Alphabetical team index">', paste(letter_links, collapse = ""), '</div>',
    '</div>',
    '<div class="team-empty" id="team-empty">No team members match your search.</div>',
    '<section class="team-group" aria-label="RedeC2 team members">',
      all_cards,
    '</section>'
  )
)
```

```{=html}
<script>
document.addEventListener("DOMContentLoaded", function() {
  const searchInput = document.getElementById("team-search");
  const empty = document.getElementById("team-empty");

  if (!searchInput) return;

  searchInput.addEventListener("input", function() {
    const term = this.value.toLowerCase().trim();
    const cards = document.querySelectorAll(".team-card");
    let visible = 0;

    cards.forEach(function(card) {
      const haystack = ((card.dataset.name || "") + " " + (card.dataset.institution || "")).toLowerCase();
      const show = haystack.includes(term);
      card.style.display = show ? "" : "none";
      if (show) visible += 1;
    });

    if (empty) empty.style.display = visible ? "none" : "block";
  });
});
</script>
```

```{=html}
<script>
document.addEventListener("DOMContentLoaded", function () {
  const revealTargets = document.querySelectorAll(
    ".rc2-section, .content-card, .rc2-page-hero, .team-controls, .team-card"
  );

  revealTargets.forEach((el, index) => {
    el.classList.add("reveal-ready");
    el.style.setProperty("--reveal-delay", `${Math.min(index, 8) * 45}ms`);
  });

  if (
    window.matchMedia("(prefers-reduced-motion: reduce)").matches ||
    !("IntersectionObserver" in window)
  ) {
    revealTargets.forEach(el => el.classList.add("is-visible"));
  } else {
    const revealObserver = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
          revealObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.12, rootMargin: "0px 0px -8% 0px" });

    revealTargets.forEach(el => revealObserver.observe(el));
  }

  const sections = document.querySelectorAll("section[id]");
  const navLinks = document.querySelectorAll(".navbar a[href]");

  if (sections.length && "IntersectionObserver" in window) {
    const navObserver = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;

        const id = entry.target.id;

        navLinks.forEach(link => {
          const href = link.getAttribute("href") || "";
          link.classList.toggle("active-section", href.endsWith("#" + id));
        });
      });
    }, { rootMargin: "-35% 0px -55% 0px", threshold: 0 });

    sections.forEach(section => navObserver.observe(section));
  }
});
</script>
```

---
title: "Sites - Rede C2"
subtitle: "Permanent plots and environmental gradients across Caatinga and Cerrado"
format:
  html:
    toc: false
    page-layout: full
execute:
  echo: false
  eval: true
  freeze: false
  cache: false
  warning: false
  message: false
resources:
  - ../figures/
  - ../_input_data/
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)

find_up <- function(rel_path, start = getwd(), max_levels = 8) {
  d <- normalizePath(start, winslash = "/", mustWork = FALSE)
  for (i in 0:max_levels) {
    cand <- file.path(d, rel_path)
    if (file.exists(cand)) return(cand)
    parent <- dirname(d)
    if (identical(parent, d)) break
    d <- parent
  }
  NA_character_
}
```

```{=html}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800;900&family=Source+Serif+4:opsz,wght@8..60,400;8..60,600;8..60,700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
```

```{=html}
<style>
:root {
  --rc2-red: #A93632;
  --rc2-red-dark: #6E1F1D;
  --rc2-green: #626A38;
  --rc2-green-dark: #2E3B24;
  --rc2-green-soft: #D7DDC5;
  --rc2-ochre: #D99A27;
  --rc2-cream: #F6F1E8;
  --rc2-paper: #FFFDF7;
  --rc2-ink: #171717;
  --rc2-muted: #675F55;
  --rc2-line: rgba(98,106,56,0.22);
  --rc2-shadow: 0 16px 34px rgba(0,0,0,0.12);

  --rc2-safe-gutter: clamp(0.75rem, 2.6vw, 2.25rem);
  --rc2-content-max: 1180px;
  --rc2-wide-max: 1480px;
}

*, *::before, *::after {
  box-sizing: border-box;
}

html,
body {
  width: 100% !important;
  max-width: 100% !important;
  overflow-x: clip !important;
  scroll-behavior: smooth;
}

@supports not (overflow: clip) {
  html,
  body {
    overflow-x: hidden !important;
  }
}

body {
  margin: 0 !important;
  background:
    linear-gradient(rgba(246,241,232,0.90), rgba(246,241,232,0.86)),
    url("../figures/itirapina.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  background-attachment: fixed;
  background-position: center top;
  color: var(--rc2-ink);
  font-family: 'Montserrat', Arial, sans-serif;
}

img,
video,
iframe,
svg,
canvas {
  max-width: 100%;
}

a {
  color: var(--rc2-red);
}

#quarto-content,
#quarto-document-content,
main,
main.content,
.page-columns,
.page-columns .content {
  min-width: 0 !important;
  max-width: none !important;
  overflow-x: visible !important;
}

#quarto-content,
#quarto-document-content,
main,
main.content,
.page-columns,
.page-columns .content {
  margin-left: 0 !important;
  margin-right: 0 !important;
  padding-left: 0 !important;
  padding-right: 0 !important;
}

main.content {
  background: transparent;
  width: 100% !important;
  padding: 0 0 3.5rem 0 !important;
}

.quarto-title-block,
#title-block-header {
  display: none !important;
}

/* Navbar */
.navbar {
  box-shadow: 0 2px 18px rgba(0,0,0,0.10);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

.navbar .navbar-brand,
.navbar .nav-link {
  font-family: 'Montserrat', Arial, sans-serif;
}

.navbar .nav-link.active-section {
  color: var(--rc2-red) !important;
  font-weight: 800;
  border-bottom: 2px solid var(--rc2-red);
}

/* =========================================================
   HERO · full frame, clean
   ========================================================= */
.rc2-page-hero {
  position: relative;
  left: 50%;
  right: 50%;
  width: 100vw !important;
  max-width: 100vw !important;
  min-height: calc(100svh - 88px);
  margin-top: 0 !important;
  margin-bottom: 0 !important;
  margin-left: -50vw !important;
  margin-right: -50vw !important;
  display: grid;
  align-items: center;
  overflow: hidden;
  isolation: isolate;
  color: #fff;
  background:
    linear-gradient(108deg, rgba(23,23,23,0.56) 0%, rgba(46,59,36,0.34) 52%, rgba(110,31,29,0.22) 100%),
    url("../figures/itirapina.jpg");
  background-size: cover;
  background-position: center center;
  background-repeat: no-repeat;
}

.page-columns .content > .rc2-page-hero,
#quarto-document-content > .rc2-page-hero,
main.content > .rc2-page-hero {
  grid-column: screen-start / screen-end !important;
}

.rc2-page-hero::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: 0;
  background:
    linear-gradient(
      180deg,
      rgba(0,0,0,0.18) 0%,
      rgba(0,0,0,0.05) 48%,
      rgba(0,0,0,0.34) 100%
    );
  pointer-events: none;
}

.rc2-page-hero-inner {
  position: relative;
  z-index: 2;
  width: min(1120px, calc(100% - var(--rc2-safe-gutter) * 2));
  margin: 0 auto;
  padding: clamp(2.4rem, 6vh, 4.5rem) 0;
}

.rc2-title-rule {
  width: min(300px, 46vw);
  height: 5px;
  margin: 0 0 clamp(1rem, 2vw, 1.35rem);
  border-radius: 999px;
  background: linear-gradient(90deg, var(--rc2-ochre), rgba(246,241,232,0.24));
  animation: fadeSlideUp 0.75s 0.08s ease both;
}

.rc2-page-hero h1 {
  margin: 0;
  max-width: 1000px;
  color: #fff;
  font-family: 'Montserrat', Arial, sans-serif;
  font-size: clamp(3.6rem, 8.4vw, 8.2rem);
  line-height: 0.88;
  font-weight: 900;
  letter-spacing: -0.085em;
  text-transform: uppercase;
  text-shadow: 0 12px 36px rgba(0,0,0,0.50);
  animation: fadeSlideUp 0.75s 0.16s ease both;
}

.rc2-page-hero h1 span {
  display: block;
  color: var(--rc2-ochre);
}

.rc2-page-hero p {
  max-width: 860px;
  margin: clamp(1rem, 2vw, 1.25rem) 0 0;
  color: rgba(255,253,247,0.97);
  font-family: 'Source Serif 4', Georgia, serif;
  font-size: clamp(1.18rem, 2.15vw, 1.9rem);
  line-height: 1.25;
  font-weight: 700;
  text-shadow: 0 4px 18px rgba(0,0,0,0.48);
  animation: fadeSlideUp 0.75s 0.24s ease both;
}

/* =========================================================
   SECTIONS
   ========================================================= */
.rc2-section,
.content-card {
  width: min(var(--rc2-content-max), calc(100% - var(--rc2-safe-gutter) * 2));
  margin: 2.4rem auto 0;
  padding: clamp(1.35rem, 3vw, 2.5rem);
  background:
    radial-gradient(circle at 100% 0%, rgba(217,154,39,0.08), transparent 32%),
    linear-gradient(180deg, rgba(255,253,247,0.97), rgba(246,241,232,0.90));
  border: 1px solid rgba(98,106,56,0.18);
  border-top: 5px solid var(--rc2-green);
  box-shadow: var(--rc2-shadow);
  overflow: hidden;
}

.rc2-section--wide {
  width: min(var(--rc2-wide-max), calc(100% - var(--rc2-safe-gutter) * 2));
}

.rc2-section-title {
  display: grid;
  grid-template-columns: 90px 1fr;
  gap: 0.8rem;
  align-items: center;
  margin-bottom: 1.35rem;
}

.rc2-section-title::before {
  content: "";
  height: 1px;
  background: var(--rc2-ink);
  opacity: 0.72;
}

.rc2-section-title h2 {
  margin: 0;
  padding-bottom: 0.45rem;
  border-bottom: 1px solid rgba(98,106,56,0.18);
  color: var(--rc2-red);
  font-family: 'Montserrat', Arial, sans-serif;
  font-size: clamp(1.25rem, 3vw, 2.1rem);
  font-weight: 900;
  line-height: 1.05;
  letter-spacing: -0.045em;
  text-transform: uppercase;
}

.rc2-text {
  max-width: 980px;
}

.rc2-lead,
.lead {
  margin: 0 0 1.15rem 0 !important;
  padding-left: 1rem;
  border-left: 5px solid var(--rc2-ochre);
  color: #191711 !important;
  font-family: 'Source Serif 4', Georgia, serif !important;
  font-size: clamp(1.08rem, 2vw, 1.3rem) !important;
  line-height: 1.65 !important;
}

.rc2-section p,
.content-card p {
  color: var(--rc2-ink);
  font-size: 0.98rem;
  line-height: 1.75;
}

/* =========================================================
   IMAGE CAROUSEL
   ========================================================= */
.rc2-carousel-shell {
  width: min(var(--rc2-wide-max), calc(100% - var(--rc2-safe-gutter) * 2));
  margin: 2.4rem auto 0;
  overflow: hidden;
  border-radius: 28px;
  box-shadow: var(--rc2-shadow);
  background: rgba(255,253,247,0.84);
  border: 1px solid rgba(98,106,56,0.18);
}

.rc2-carousel-track {
  display: flex;
  gap: 1rem;
  width: max-content;
  padding: 1rem;
  animation: rc2-marquee 56s linear infinite;
}

.rc2-carousel-shell:hover .rc2-carousel-track {
  animation-play-state: paused;
}

.rc2-carousel-item {
  position: relative;
  flex: 0 0 auto;
  width: clamp(260px, 28vw, 450px);
  height: 250px;
  margin: 0;
  overflow: hidden;
  border-radius: 20px;
  background: var(--rc2-green-dark);
  box-shadow: 0 12px 24px rgba(0,0,0,0.13);
}

.rc2-carousel-item img {
  width: 100%;
  height: 100%;
  display: block;
  object-fit: cover;
  transition: transform 0.35s ease, filter 0.35s ease;
}

.rc2-carousel-item::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, transparent 35%, rgba(0,0,0,0.68) 100%);
  opacity: 0;
  transition: opacity 0.25s ease;
  pointer-events: none;
}

.rc2-carousel-item figcaption {
  position: absolute;
  left: 0.85rem;
  right: 0.85rem;
  bottom: 0.75rem;
  z-index: 2;
  color: #fff;
  font-family: 'Space Mono', monospace;
  font-size: 0.74rem;
  font-weight: 900;
  letter-spacing: 0.08em;
  line-height: 1.25;
  text-transform: uppercase;
  text-shadow: 0 3px 12px rgba(0,0,0,0.42);
  opacity: 0;
  transform: translateY(8px);
  transition: opacity 0.25s ease, transform 0.25s ease;
}

.rc2-carousel-item:hover img,
.rc2-carousel-item:focus-within img {
  transform: scale(1.045);
  filter: saturate(1.04) contrast(1.06);
}

.rc2-carousel-item:hover::after,
.rc2-carousel-item:focus-within::after,
.rc2-carousel-item:hover figcaption,
.rc2-carousel-item:focus-within figcaption {
  opacity: 1;
  transform: translateY(0);
}

@keyframes rc2-marquee {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}

/* =========================================================
   MAP
   ========================================================= */
.leaflet-container {
  border-radius: 24px;
  overflow: hidden;
  box-shadow: 0 16px 34px rgba(0,0,0,0.14);
  font-family: 'Montserrat', Arial, sans-serif;
}

.leaflet-control,
.leaflet-bar,
.leaflet-control-layers {
  border-radius: 14px !important;
  box-shadow: 0 10px 18px rgba(0,0,0,0.12) !important;
  border: 1px solid rgba(98,106,56,0.22) !important;
  background: rgba(255,253,247,0.96) !important;
}

.leaflet-popup-content-wrapper {
  border-radius: 18px !important;
  background: rgba(255,253,247,0.98) !important;
  border: 1px solid rgba(98,106,56,0.24) !important;
}

.popup-card { max-width: 330px; }

.popup-title {
  font-weight: 900;
  font-size: 1rem;
  color: var(--rc2-green-dark);
  margin: 0 0 0.35rem 0;
}

.popup-sub {
  font-family: 'Space Mono', monospace;
  font-size: 0.64rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #7A5A18;
  margin-bottom: 0.6rem;
}

.popup-row {
  display: grid;
  grid-template-columns: 90px 1fr;
  gap: 0.4rem;
  font-size: 0.86rem;
  margin: 0.22rem 0;
}

.popup-k {
  font-family: 'Space Mono', monospace;
  font-size: 0.62rem;
  text-transform: uppercase;
  color: #7A5A18;
}

.popup-v { color: #312719; }

.map-warning {
  padding: 1rem;
  border-radius: 18px;
  background: rgba(169,54,50,0.08);
  border: 1px dashed rgba(169,54,50,0.35);
  color: var(--rc2-red-dark);
}

/* =========================================================
   ANIMATION
   ========================================================= */
@keyframes fadeSlideUp {
  from { opacity: 0; transform: translateY(24px); }
  to   { opacity: 1; transform: translateY(0); }
}

.reveal-ready {
  opacity: 0;
  transform: translateY(38px);
  transition: opacity 0.72s ease, transform 0.72s cubic-bezier(.2,.7,.2,1);
  transition-delay: var(--reveal-delay, 0ms);
  will-change: opacity, transform;
}

.reveal-ready.is-visible {
  opacity: 1;
  transform: translateY(0);
}

@media (prefers-reduced-motion: reduce) {
  .reveal-ready,
  .rc2-carousel-track {
    opacity: 1 !important;
    transform: none !important;
    transition: none !important;
    animation: none !important;
  }
}

/* =========================================================
   RESPONSIVE
   ========================================================= */
@media (max-height: 760px) and (min-width: 900px) {
  .rc2-page-hero h1 {
    font-size: clamp(3.2rem, 7.2vw, 6.8rem);
  }

  .rc2-page-hero p {
    font-size: clamp(1.05rem, 1.85vw, 1.6rem);
  }
}

@media (max-width: 980px) {
  .rc2-section-title {
    grid-template-columns: 56px 1fr;
  }
}

@media (max-width: 760px) {
  .rc2-page-hero {
    min-height: calc(100svh - 76px);
  }

  .rc2-page-hero h1 {
    font-size: clamp(3rem, 15vw, 5.4rem);
  }

  .rc2-carousel-shell {
    border-radius: 20px;
  }

  .rc2-carousel-item {
    width: 82vw;
    height: 220px;
  }

  .rc2-section-title {
    grid-template-columns: 34px 1fr;
  }
}

@media (max-width: 420px) {
  :root { --rc2-safe-gutter: 0.65rem; }

  .rc2-section,
  .content-card {
    padding: clamp(1rem, 4vw, 1.35rem);
    border-radius: 22px;
  }
}

.html-widget,
.leaflet,
.leaflet-container,
iframe {
  width: 100% !important;
  max-width: 100% !important;
}
</style>
```

```{=html}
<section class="rc2-page-hero" id="sites-hero" aria-label="RedeC2 field sites">
  <div class="rc2-page-hero-inner">
    <div class="rc2-title-rule" aria-hidden="true"></div>

    <h1>
      Sites
      <span>& gradients</span>
    </h1>

    <p>
      Field sites and environmental gradients connecting Caatinga and Cerrado
      vegetation across Rede C2.
    </p>
  </div>
</section>
```

```{=html}
<section class="rc2-section" id="overview">
  <div class="rc2-section-title"><h2>Spatial structure</h2></div>

  <div class="rc2-text">
    <p class="rc2-lead">
      Rede C2 field sites represent local monitoring efforts distributed across
      seasonally dry ecosystems, linking permanent plots to vegetation type,
      landscape context, soils and climate.
    </p>

    <p>
      The spatial organization of the network supports comparisons among
      Caatinga and Cerrado sites and helps interpret how plant communities vary
      across environmental gradients and through repeated censuses.
    </p>
  </div>
</section>
```

```{=html}
<div class="rc2-carousel-shell" aria-label="Rede C2 site image carousel">
  <div class="rc2-carousel-track">

    <figure class="rc2-carousel-item" title="Canudos">
      <img src="../figures/canudos_dc.jpg" alt="Canudos" loading="lazy" decoding="async">
      <figcaption>Canudos</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="Gruta dos Brejões">
      <img src="../figures/gruta_brejoes_dc.jpg" alt="Gruta dos Brejões" loading="lazy" decoding="async">
      <figcaption>Gruta dos Brejões</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="São Francisco">
      <img src="../figures/s_francisco_dc.jpg" alt="São Francisco" loading="lazy" decoding="async">
      <figcaption>São Francisco</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="Água Limpa">
      <img src="../figures/agua_limpa.jpg" alt="Água Limpa" loading="lazy" decoding="async">
      <figcaption>Água Limpa</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="Itirapina">
      <img src="../figures/itirapina.jpg" alt="Itirapina" loading="lazy" decoding="async">
      <figcaption>Itirapina</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="Água Limpa">
      <img src="../figures/agua_limpa_1.jpg" alt="Água Limpa" loading="lazy" decoding="async">
      <figcaption>Água Limpa</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="Canudos">
      <img src="../figures/canudos_dc.jpg" alt="Canudos" loading="lazy" decoding="async">
      <figcaption>Canudos</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="Gruta dos Brejões">
      <img src="../figures/gruta_brejoes_dc.jpg" alt="Gruta dos Brejões" loading="lazy" decoding="async">
      <figcaption>Gruta dos Brejões</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="São Francisco">
      <img src="../figures/s_francisco_dc.jpg" alt="São Francisco" loading="lazy" decoding="async">
      <figcaption>São Francisco</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="Água Limpa">
      <img src="../figures/agua_limpa.jpg" alt="Água Limpa" loading="lazy" decoding="async">
      <figcaption>Água Limpa</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="Itirapina">
      <img src="../figures/itirapina.jpg" alt="Itirapina" loading="lazy" decoding="async">
      <figcaption>Itirapina</figcaption>
    </figure>

    <figure class="rc2-carousel-item" title="Água Limpa">
      <img src="../figures/agua_limpa_1.jpg" alt="Água Limpa" loading="lazy" decoding="async">
      <figcaption>Água Limpa</figcaption>
    </figure>

  </div>
</div>
```

```{=html}
<section class="rc2-section" id="mapa">
  <div class="rc2-section-title"><h2>Interactive map</h2></div>

  <p class="rc2-lead">
    The map presents Rede C2 permanent plots with vegetation classification,
    plot area, census information and climatic or institutional metadata
    available in the project table.
  </p>
```

```{r}
library(htmltools)

csv_path <- find_up(file.path("_input_data", "plotMetadata_R2C.csv"))

if (is.na(csv_path)) {
  HTML('<div class="map-warning">The file <strong>_input_data/plotMetadata_R2C.csv</strong> was not found. Keep this file in the project root to render the interactive map.</div>')
} else {
  library(leaflet)
  library(dplyr)

  plotMetadata_R2C <- read.csv(csv_path)

  plotMetadata_R2C$biome <- "Unclassified"
  plotMetadata_R2C$biome[plotMetadata_R2C$kyle_veg_broad == "Forest"] <- "Caatinga"
  plotMetadata_R2C$biome[plotMetadata_R2C$kyle_veg_broad == "Savanna"] <- "Cerrado"
  plotMetadata_R2C$biome[plotMetadata_R2C$plot_id %in% c("CIP-21", "CIP-22", "CIP-23", "PNP-04")] <- "Cerrado"

  plotMetadata_R2C$longitude[plotMetadata_R2C$longitude > 0] <- -plotMetadata_R2C$longitude[plotMetadata_R2C$longitude > 0]
  plotMetadata_R2C$latitude[plotMetadata_R2C$latitude > 0] <- -plotMetadata_R2C$latitude[plotMetadata_R2C$latitude > 0]

  pal <- colorFactor(
    c("#A93632", "#626A38", "#D99A27"),
    domain = c("Caatinga", "Cerrado", "Unclassified")
  )

  popup_html <- paste0(
    '<div class="popup-card">',
    '<div class="popup-title">', htmlEscape(plotMetadata_R2C$plot_id), '</div>',
    '<div class="popup-sub">', htmlEscape(plotMetadata_R2C$biome), '</div>',
    '<div class="popup-row"><span class="popup-k">Area</span><span class="popup-v">', htmlEscape(plotMetadata_R2C$plot_area), ' ha</span></div>',
    '<div class="popup-row"><span class="popup-k">Census</span><span class="popup-v">', htmlEscape(plotMetadata_R2C$n_census), '</span></div>',
    '<div class="popup-row"><span class="popup-k">BIO12</span><span class="popup-v">', htmlEscape(plotMetadata_R2C$bio12), '</span></div>',
    '<div class="popup-row"><span class="popup-k">PI</span><span class="popup-v">', htmlEscape(plotMetadata_R2C$prinv), '</span></div>',
    '</div>'
  )

  leaflet(
    data = plotMetadata_R2C,
    width = "100%",
    height = 720,
    options = leafletOptions(minZoom = 2, maxZoom = 22)
  ) %>%
    addProviderTiles(providers$CartoDB.Positron, group = "Light") %>%
    addProviderTiles(providers$Esri.WorldImagery, group = "Satellite") %>%
    addProviderTiles(providers$OpenTopoMap, group = "Topo") %>%
    addCircleMarkers(
      ~longitude,
      ~latitude,
      radius = 7,
      stroke = TRUE,
      weight = 2,
      color = "#FFFDF7",
      fillColor = ~pal(biome),
      fillOpacity = 0.92,
      label = ~paste0(plot_id, " · ", biome),
      popup = popup_html,
      group = "Plots"
    ) %>%
    addLayersControl(
      baseGroups = c("Light", "Satellite", "Topo"),
      overlayGroups = c("Plots"),
      position = "bottomleft",
      options = layersControlOptions(collapsed = TRUE)
    ) %>%
    addLegend(
      "bottomright",
      pal = pal,
      values = ~biome,
      title = "Vegetation",
      opacity = 0.9
    )
}
```

```{=html}
</section>
```

```{=html}
<section class="rc2-section" id="gradients">
  <div class="rc2-section-title"><h2>Environmental context</h2></div>

  <div class="rc2-text">
    <p>
      Site-level information can be interpreted together with climate, soil,
      vegetation structure and landscape descriptors. Repeated censuses connect
      local dynamics with broader ecological patterns across dry forests and
      savannas.
    </p>
  </div>
</section>
```

```{=html}
<script>
document.addEventListener("DOMContentLoaded", function () {
  const revealTargets = document.querySelectorAll(
    ".rc2-section, .content-card, .rc2-page-hero, .rc2-carousel-shell, .team-controls, .team-card"
  );

  revealTargets.forEach((el, index) => {
    el.classList.add("reveal-ready");
    el.style.setProperty("--reveal-delay", `${Math.min(index, 8) * 45}ms`);
  });

  if (
    window.matchMedia("(prefers-reduced-motion: reduce)").matches ||
    !("IntersectionObserver" in window)
  ) {
    revealTargets.forEach(el => el.classList.add("is-visible"));
  } else {
    const revealObserver = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
          revealObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.12, rootMargin: "0px 0px -8% 0px" });

    revealTargets.forEach(el => revealObserver.observe(el));
  }

  const sections = document.querySelectorAll("section[id]");
  const navLinks = document.querySelectorAll(".navbar a[href]");

  if (sections.length && "IntersectionObserver" in window) {
    const navObserver = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;

        const id = entry.target.id;

        navLinks.forEach(link => {
          const href = link.getAttribute("href") || "";
          link.classList.toggle("active-section", href.endsWith("#" + id));
        });
      });
    }, { rootMargin: "-35% 0px -55% 0px", threshold: 0 });

    sections.forEach(section => navObserver.observe(section));
  }
});
</script>
```
